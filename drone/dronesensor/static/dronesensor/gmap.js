var map;
function initMap() {
  map = new google.maps.Map(document.getElementById('map'), {
    center: {lat: -34.397, lng: 150.644},
    zoom: 8
  });
  var location1 = {lat: -34.397, lng: 150.644}
  var marker = new google.maps.Marker ({
    position: location1,
    map: map,
    title: 'lol'
    // get information get co-ordinates 
    var temepraturePoint {
      gpsPosition: 
      teperature: 
    }

    //loop 
      //temepraturePoint.gpsPosition = shadow.get()
      // temepraturePoint.teperature = shadow.get()
    // or use latLong
  })
}